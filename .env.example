# Optional backend origin for future AI requests.
# The Vite-prefixed variable is a public URL only — never a private API key.
# Private secrets belong on the server that implements POST /api/ai.
VITE_API_BASE_URL=
